# Enhanced Qwen Typo Fixer Training Requirements
# Updated for dual RTX5090 training with T5 improvements

# Core ML libraries
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.14.0
safetensors>=0.4.0

# Training and evaluation
accelerate>=0.20.0
evaluate>=0.4.0
wandb>=0.15.0
tensorboard>=2.14.0

# Data processing
tqdm>=4.65.0
numpy>=1.21.0
pandas>=1.5.0
pyarrow>=10.0.0

# HuggingFace Hub
huggingface_hub>=0.17.0

# Utilities
pyyaml>=6.0
requests>=2.28.0
packaging>=21.0

# Optional GPU optimizations
# bitsandbytes>=0.41.0  # Uncomment for 8-bit training
# flash-attn>=2.0.0     # Uncomment for flash attention (requires CUDA)