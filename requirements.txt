# Enhanced Qwen Typo Fixer Training Requirements
# MINIMAL DEPS - Safe for preconfigured RTX5090 machines
# Assumes torch, CUDA libraries already installed

# Essential libraries only (avoid downgrading preinstalled GPU stack)
transformers>=4.30.0
datasets>=2.10.0
huggingface_hub>=0.16.0

# Data processing - lightweight
tqdm>=4.60.0

# Training utilities (optional - comment out if causing issues)
# accelerate  # Use system version if available
# wandb      # Only if experiment tracking needed