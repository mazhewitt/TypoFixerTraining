# ByT5 Typo Fixer Training Requirements
# Uses >= to avoid downgrading preinstalled libraries

# Core ML libraries (use >= to preserve existing versions)
torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0
tokenizers>=0.13.0

# Essential training dependencies  
accelerate>=0.26.0
evaluate>=0.4.0
sacrebleu>=2.3.0

# Data processing
pandas>=1.5.0
numpy>=1.24.0
tqdm>=4.65.0

# HuggingFace Hub
huggingface-hub>=0.16.0

# Utilities
requests>=2.28.0
packaging>=21.0
safetensors>=0.3.0

# Optional but recommended
# wandb>=0.15.0  # Experiment tracking
# jupyter>=1.0.0  # Notebooks
# tensorboard>=2.13.0  # Monitoring