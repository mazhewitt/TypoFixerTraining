# Core ML training requirements
torch>=2.0.0
transformers>=4.30.0
datasets>=2.14.0
tokenizers>=0.13.0
accelerate>=0.20.0
numpy>=1.24.0
tqdm>=4.65.0
scikit-learn>=1.3.0

# Hugging Face Hub integration
huggingface_hub>=0.16.0

# ANE deployment (macOS only)
coremltools>=7.0

# Training utilities  
safetensors>=0.3.0
wandb  # Optional: for experiment tracking

# Cloud training extras (install with: pip install -r requirements-cloud.txt)
# jupyter
# tensorboard