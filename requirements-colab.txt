# Optimized requirements for Google Colab 
# Uses pre-installed PyTorch to avoid NVIDIA library updates

# Core ML libraries (skip torch - use Colab's version)
transformers>=4.30.0
datasets>=2.14.0
tokenizers>=0.13.0
accelerate>=0.20.0
# numpy  # Already in Colab
# tqdm   # Already in Colab
scikit-learn>=1.3.0

# Hugging Face Hub integration
huggingface_hub>=0.16.0

# Training utilities  
safetensors>=0.3.0
# wandb  # Already in Colab

# Cloud-specific tools
jupyter>=1.0.0
tensorboard>=2.14.0
ipywidgets>=8.0.0
nvidia-ml-py3  # GPU monitoring
ipython>=8.0.0
matplotlib>=3.7.0
seaborn>=0.12.0